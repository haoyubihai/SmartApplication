<manifest package="aly.library.common.base"
          xmlns:android="http://schemas.android.com/apk/res/android">
<uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- camera 权限列表 start-->

    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Have developers add this. We don't want AUDIO permission to be auto-added to
         apps that just want to take pictures. -->
    <!-- uses-permission android:name="android.permission.RECORD_AUDIO" /-->

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false"/>

    <application>
        <activity android:name="jrh.library.common.filepicker.activity.SelectFileByBrowserActivity" />
        <activity android:name="jrh.library.common.filepicker.activity.SelectPictureActivity" />
        <activity android:name="jrh.library.common.filepicker.activity.SelectFileByScanActivity" />

        <activity android:name="jrh.library.common.filepicker.activity.PicturesViewActivity"/>
    </application>

    <!-- camera 权限列表 end-->

</manifest>
